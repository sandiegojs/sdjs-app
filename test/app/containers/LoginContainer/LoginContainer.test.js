const assert = require('chai').assert;
const LoginContainer = require ('../../../../app/containers/LoginContainer/LoginContainer.js')

test('it sends information of the user', () => {
    function LoginContainer(data) {
    expect(data).toBe('')}
})